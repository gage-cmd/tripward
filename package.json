{
  "name": "fusecap",
  "version": "0.1.0-alpha",
  "description": "Tripward — local circuit breaker for Claude Code — Days 8–14 paid beta",
  "type": "module",
  "bin": {
    "tripward": "./dist/cli.js",
    "fusecap": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "tripward": "tsx src/cli.ts",
    "fusecap": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
